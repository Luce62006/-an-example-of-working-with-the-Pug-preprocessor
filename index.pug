-
 const n = (i=10) => {
  let result = -1;
  let progression = [];
  for (let counter = 0; counter <= i; counter += 1) {
   result = result + 2;
   progression.push(result)
  };
  return progression;
 };
 const items = n();

ul
 each item in items
         li= item

